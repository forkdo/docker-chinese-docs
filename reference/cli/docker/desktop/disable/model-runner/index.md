# docker desktop disable model-runner

# docker desktop disable model-runner

禁用模型运行器

## 摘要

```
docker desktop disable model-runner [flags]
```

## 选项

```
      --all-namespaces   禁用所有命名空间中的模型运行器
  -h, --help             model-runner disable 命令的帮助
```
