---
title: 什么是容器？
weight: 10
keywords: 概念, 构建, 镜像, 容器, docker desktop
description: 什么是容器？这个概念页面将向你介绍容器，并提供一个快速上手实践，让你运行第一个容器。
aliases:
- /guides/walkthroughs/what-is-a-container/
- /guides/walkthroughs/run-a-container/
- /guides/walkthroughs/
- /get-started/run-your-own-container/
- /get-started/what-is-a-container/
- /guides/docker-concepts/the-basics/what-is-a-container/
---

{{< youtube-embed W1kWqFkiu7k >}}

## 解释

想象一下，你正在开发一个杀手级的 Web 应用，它有三个主要组件——React 前端、Python API 和 PostgreSQL 数据库。如果你想开发这个项目，就需要安装 Node、Python 和 PostgreSQL。

如何确保你和团队中的其他开发者、CI/CD 系统，甚至生产环境使用的是相同版本？如何确保你的应用所需的 Python（或 Node 或数据库）版本不会受到机器上已有内容的影响？如何管理潜在的冲突？

容器来了！

什么是容器？简单来说，容器是每个应用组件的隔离进程。每个组件——前端 React 应用、Python API 引擎和数据库——都在自己的隔离环境中运行，完全独立于机器上的其他所有内容。

以下是它们的酷炫之处。容器具有：

- **自包含性**。每个容器都包含运行所需的一切，不依赖主机上的任何预安装依赖项。
- **隔离性**。由于容器是隔离运行的，它们对主机和其他容器的影响极小，提高了应用的安全性。
- **独立性**。每个容器都是独立管理的。删除一个容器不会影响其他容器。
- **可移植性**。容器可以随时随地运行！在你的开发机器上运行的容器，在数据中心或云环境中也能以相同方式工作！

### 容器与虚拟机（VM）的对比

简单来说，虚拟机是一个完整的操作系统，拥有自己的内核、硬件驱动、程序和应用。为了隔离单个应用而启动一个虚拟机，开销很大。

而容器只是一个隔离的进程，以及运行所需的所有文件。如果你运行多个容器，它们都共享同一个内核，让你能在更少的基础设施上运行更多应用。

> **虚拟机和容器一起使用**
>
> 通常，你会看到虚拟机和容器一起使用。例如，在云环境中，提供的机器通常是虚拟机。但与其配置一台机器运行一个应用，不如在虚拟机上安装容器运行时，运行多个容器化应用，从而提高资源利用率并降低成本。

## 动手尝试

在这个实践中，你将学习如何使用 Docker Desktop GUI 运行 Docker 容器。

{{< tabs group=concept-usage persist=true >}}
{{< tab name="使用 GUI" >}}

使用以下说明运行一个容器：

1. 打开 Docker Desktop，在顶部导航栏选择 **Search** 字段。

2. 在搜索输入框中输入 `welcome-to-docker`，然后选择 **Pull** 按钮。

    ![Docker Desktop Dashboard 显示 welcome-to-docker Docker 镜像搜索结果的截图](images/search-the-docker-image.webp?border=true&w=1000&h=700)

3. 镜像成功拉取后，选择 **Run** 按钮。

4. 展开 **Optional settings**。

5. 在 **Container name** 中输入 `welcome-to-docker`。

6. 在 **Host port** 中输入 `8080`。

    ![Docker Desktop Dashboard 显示容器运行对话框，容器名称为 welcome-to-docker，端口号为 8080 的截图](images/run-a-new-container.webp?border=true&w=550&h=400)

7. 选择 **Run** 启动你的容器。

恭喜！你刚刚运行了你的第一个容器！🎉

### 查看你的容器

你可以通过 Docker Desktop Dashboard 的 **Containers** 视图查看所有容器。

![Docker Desktop GUI 容器视图的截图，显示 welcome-to-docker 容器在主机端口 8080 上运行](images/view-your-containers.webp?border=true&w=750&h=600)

此容器运行一个 Web 服务器，显示一个简单的网站。在处理更复杂的项目时，你会在不同的容器中运行不同的部分。例如，前端、后端和数据库可能分别运行在不同的容器中。

### 访问前端

启动容器时，你将容器的一个端口暴露到了你的机器上。可以将其理解为创建配置，让你能够连接到容器的隔离环境。

对于此容器，前端可通过端口 `8080` 访问。要打开网站，请选择容器 **Port(s)** 列中的链接，或在浏览器中访问 [http://localhost:8080](http://localhost:8080)。

![运行容器返回的登录页面截图](images/access-the-frontend.webp?border)

### 探索你的容器

Docker Desktop 让你能够探索和与容器的不同方面进行交互。自己试试吧。

1. 进入 Docker Desktop Dashboard 的 **Containers** 视图。

2. 选择你的容器。

3. 选择 **Files** 选项卡，探索容器的隔离文件系统。

    ![Docker Desktop Dashboard 显示运行中容器内文件和目录的截图](images/explore-your-container.webp?border)

### 停止你的容器

`docker/welcome-to-docker` 容器会一直运行，直到你停止它。

1. 进入 Docker Desktop Dashboard 的 **Containers** 视图。

2. 找到你想停止的容器。

3. 在 **Actions** 列中选择 **Stop** 操作。

    ![Docker Desktop Dashboard 截图，显示选中的 welcome 容器准备停止](images/stop-your-container.webp?border)

{{< /tab >}}
{{< tab name="使用 CLI" >}}

按照以下说明使用 CLI 运行容器：

1. 打开你的 CLI 终端，使用 [`docker run`](/reference/cli/docker/container/run/) 命令启动一个容器：

    ```console
    $ docker run -d -p 8080:80 docker/welcome-to-docker
    ```

    此命令的输出是完整的容器 ID。

恭喜！你刚刚启动了你的第一个容器！🎉

### 查看你的运行中容器

你可以使用 [`docker ps`](/reference/cli/docker/container/ls/) 命令验证容器是否正在运行：

```console
docker ps
```

你会看到类似以下的输出：

```console
 CONTAINER ID   IMAGE                      COMMAND                  CREATED          STATUS          PORTS                      NAMES
 a1f7a4bb3a27   docker/welcome-to-docker   "/docker-entrypoint.…"   11 seconds ago   Up 11 seconds   0.0.0.0:8080->80/tcp       gracious_keldysh
```

此容器运行一个 Web 服务器，显示一个简单的网站。在处理更复杂的项目时，你会在不同的容器中运行不同的部分。例如，`frontend`、`backend` 和 `database` 可能分别运行在不同的容器中。

> [!TIP]
>
> `docker ps` 命令只显示正在运行的容器。要查看已停止的容器，请添加 `-a` 标志列出所有容器：`docker ps -a`

### 访问前端

启动容器时，你将容器的一个端口暴露到了你的机器上。可以将其理解为创建配置，让你能够连接到容器的隔离环境。

对于此容器，前端可通过端口 `8080` 访问。要打开网站，请选择容器 **Port(s)** 列中的链接，或在浏览器中访问 [http://localhost:8080](http://localhost:8080)。

![运行容器中 Nginx Web 服务器的登录页面截图](images/access-the-frontend.webp?border)

### 停止你的容器

`docker/welcome-to-docker` 容器会一直运行，直到你停止它。你可以使用 `docker stop` 命令停止容器。

1. 运行 `docker ps` 获取容器 ID

2. 将容器 ID 或名称提供给 [`docker stop`](/reference/cli/docker/container/stop/) 命令：

    ```console
    docker stop <the-container-id>
    ```

> [!TIP]
>
> 引用容器时，不需要提供完整的 ID。你只需要提供足够的 ID 使其唯一即可。例如，前面的容器可以通过运行以下命令停止：
>
> ```console
> docker stop a1f
> ```

{{< /tab >}}
{{< /tabs >}}

## 额外资源

以下链接提供了关于容器的额外指导：

- [运行容器](/engine/containers/run/)
- [容器概述](https://www.docker.com/resources/what-container/)
- [为什么选择 Docker？](https://www.docker.com/why-docker/)

## 下一步

现在你已经了解了 Docker 容器的基础知识，是时候学习 Docker 镜像了。

{{< button text="什么是镜像？" url="what-is-an-image" >}}
