```markdown
---
datafolder: mcp-cli
datafile: docker_mcp_catalog_bootstrap
title: docker mcp catalog bootstrap
layout: cli
---

<!--
此页面由 Docker 的源代码自动生成。如果您想建议修改此处显示的文本，
请在 GitHub 上的源代码仓库中提交工单或拉取请求：

https://github.com/docker/mcp-gateway
-->
```

```markdown
# docker mcp catalog bootstrap

**名称**
`docker mcp catalog bootstrap` — 为 Docker MCP Catalog 引导安装服务器

**摘要**
为 Docker MCP Catalog 引导安装服务器

**语法**
```
docker mcp catalog bootstrap [OPTIONS] [SERVER]
```

**描述**
为 Docker MCP Catalog 引导安装服务器。

**选项**
| 选项 | 描述 |
|------|------|
| `--help` | 显示帮助信息 |

**相关命令**
* `docker mcp catalog bootstrap` - 为 Docker MCP Catalog 引导安装服务器

**示例**

为 Docker MCP Catalog 引导安装服务器：
```bash
docker mcp catalog bootstrap
```

**父命令**
* `docker mcp catalog` - 管理 Docker MCP Catalog