---
datafolder: desktop-cli
datafile: docker_desktop_enable_model_runner
title: docker desktop enable model-runner
layout: cli
---

## docker desktop enable model-runner

启用模型运行器

### 简介

启用模型运行器

```
docker desktop enable model-runner [flags]
```

### 选项

```
      --accept-license   接受模型运行器许可证
  -h, --help             显示 enable model-runner 命令的帮助信息
```

### 从父命令继承的选项

```
      --credentials-store string   Docker 凭据存储的名称（例如 pass、wincred）。默认值：desktop
  -d, --debug                      启用调试输出
      --timeout duration           Docker Desktop 命令超时时间（默认为 300 秒）（默认值 5m0s）
```

### 参见

* [docker desktop enable](./docker_desktop_enable.md) - 启用 Docker Desktop 功能

###### Auto generated by spf13/cobra on 11-Oct-2025