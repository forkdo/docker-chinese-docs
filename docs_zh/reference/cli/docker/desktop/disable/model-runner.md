---
datafolder: desktop-cli
datafile: docker_desktop_disable_model_runner
title: docker desktop disable model-runner
layout: cli
---

## docker desktop disable model-runner

禁用模型运行器

### 简介

禁用模型运行器

```
docker desktop disable model-runner [flags]
```

### 选项

```
  -h, --help   help for model-runner
```

### 父命令继承的选项

```
      --credentials-store string   Docker 凭据存储的路径
      --debug                      启用调试模式
      --guc-url string             Docker Desktop GUC 服务器的 URL（默认 "https://desktop.docker.com/guc"）
      --guc-version string         Docker Desktop GUC 服务器的版本（默认 "v1"）
      --guc-wait                   等待 GUC 服务器启动（默认 30s）
  -H, --host string                Docker 服务器套接字
      --timeout duration           Docker Desktop API 超时时间（默认 60s）
      --version strings            Docker Desktop API 版本（默认 [v0.1.0]）
```

### 参见

* [docker desktop disable](docker_desktop_disable.html) - 禁用 Docker Desktop 功能

###### Auto generated by spf13/cobra on 19-Sep-2025