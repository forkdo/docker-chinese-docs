> [!IMPORTANT]
>
> Docker 的文档引用并描述了 Compose V2 的功能。
>
> 自 2023 年 7 月起，Compose V1 停止接收更新，不再包含在新的 Docker Desktop 版本中。Compose V2 已取代它，现在已集成到所有当前的 Docker Desktop 版本中。更多信息，请参阅 [迁移到 Compose V2](/compose/migrate)。